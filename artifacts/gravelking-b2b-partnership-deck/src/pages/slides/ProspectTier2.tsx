import ProspectTier from '../../components/ProspectTier';

export default function ProspectTier2() {
  return <ProspectTier tier={2} title="Customer support at scale." subtitle="Conversation history, retrieval context, and agent tools create a direct optimization surface." />;
}