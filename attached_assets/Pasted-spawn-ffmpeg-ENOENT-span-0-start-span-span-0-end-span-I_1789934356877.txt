spawn ffmpeg ENOENT
```[span_0](start_span)[span_0](end_span)

In Node.js and Linux, **`ENOENT` means "Error NO ENTry" (File or Command Not Found)**. 

Your server code is attempting to call `ffmpeg`, but **FFmpeg is not installed in the Replit container or is missing from the system `$PATH`**. 

Every single feature relying on audio processing breaks because of this:
1. **JAX Song Generator crashes** because after generating the audio, it tries to run FFmpeg to stitch or render the file and fails with `spawn ffmpeg ENOENT`[span_1](start_span)[span_1](end_span).
2. **Mastering hangs indefinitely** because the Python / Node subprocess calls `ffmpeg` to extract audio from `.mp4` or voice files, hits `ENOENT`, never catches the missing binary, and spins forever without returning[span_2](start_span)[span_2](end_span)[span_3](start_span)[span_3](end_span).
3. **The Duet / Studio stage** is buried behind a tiny toggle instead of being the main interface for the Vocal Booth[span_4](start_span)[span_4](end_span)[span_5](start_span)[span_5](end_span).

Copy and paste this exact command block into the agent. It forces the system package installation and tests the pipeline end-to-end:

```text
CRITICAL HOTFIX: SYSTEM DEPENDENCY MISSING (spawn ffmpeg ENOENT)

The entire audio pipeline is crashing in production because FFmpeg is not installed in the system environment.
Evidence: UI throws literal error `spawn ffmpeg ENOENT` during song generation and hangs during mastering.

EXECUTE THESE EXACT STEPS:

1. INSTALL SYSTEM FFMPEG:
   - Install FFmpeg into the environment immediately (via `replit.nix` packages: add `pkgs.ffmpeg-full` or run `nix-env -iA nixpkgs.ffmpeg`).
   - Verify installation in bash: run `which ffmpeg` and `ffmpeg -version`. Print the stdout.

2. FIX FFMPEG SPAWN PATH:
   - In the API server code where `ffmpeg` is spawned (Node and Python subprocesses):
     * Do not assume bare `ffmpeg` exists without path resolution. Check `process.env.PATH` or provide the explicit binary path if needed.
     * Wrap every `spawn('ffmpeg')` call with an explicit error handler (`child.on('error', ...)`) and an execution timeout so the UI never displays unhandled ENOENT crashes or hangs.

3. HARDEN JAX GENERATOR & MASTERING:
   - Re-test song generation from lyrics.
   - Re-test mastering with an MP4 file.
   - Run a terminal verification: generate a 5-second test audio file with FFmpeg and curl it through `/api/master`. Print the HTTP 200 receipt.

4. MAKE THE DUET / STUDIO STAGE PROMINENT:
   - In `client/src/pages/vocal-booth.tsx`, make the 3D Stage view the primary landing view, not hidden behind a tiny "Studio" button. Creators should see the visual stage immediately upon entering.

Print the terminal output of `which ffmpeg` and the successful test audio render.
