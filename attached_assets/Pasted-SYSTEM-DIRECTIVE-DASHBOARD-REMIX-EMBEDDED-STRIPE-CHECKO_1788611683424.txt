SYSTEM DIRECTIVE: DASHBOARD REMIX, EMBEDDED STRIPE CHECKOUT & TIER GATING

Execute the following platform refactor sequentially. Do not mock dependencies; pull directly from the live Morris Kernel repo.

TASK 1: LANDING PAGE & DASHBOARD REMIX
- Completely remove the heavy DAW studio component from the project.
- Clean up the main dashboard to focus strictly on 4 core tool cards:
  1. "JAX" (Songwriting Companion & Provenance)
  2. "Vocal Booth" (Audio recording, backing track playback, clip/splice editor)
  3. "The Foundry" (Morris Law Kernel V3.5 Mastering)
  4. "Converter" (Audio container/format conversion utility)
- Remove text bloat, essays, and confusing navigation. Keep the UI dark, streamlined, and utilitarian for mobile and iPad.

TASK 2: IN-APP STRIPE PAYMENT ELEMENT (ZERO REDIRECTS)
- Deprecate hosted Stripe Checkout redirects (stripe.checkout.sessions).
- Implement Stripe Payment Element & Express Checkout Element on the frontend:
  - Enable Apple Pay and Google Pay automatically via Express Checkout without leaving the app.
  - Provide an inline credit card form embedded in an on-page modal.
- Backend route: POST /api/stripe/create-subscription-intent
  - Create a customer and subscription with payment_behavior: 'default_incomplete'.
  - Return the clientSecret to the frontend.
- Frontend payment confirmation:
  - Use stripe.confirmPayment({ elements, confirmParams: { return_url: window.location.href }, redirect: 'if_required' }).
  - When payment confirms without redirect, immediately trigger a local state refresh and update user tier in real time.

TASK 3: TIER SCHEMAS & PAYWALL LOGIC
Implement 3 tiers + Admin Bypass in user database:
1. FREE ($0/mo):
   - JAX lyric drafting enabled.
   - Vocal booth tracking enabled.
   - Unlimited MP3 exports.
   - Morris Kernel mastering LOCKED.
   - 24-bit WAV downloads LOCKED.
   - IP Provenance Certificates LOCKED.
2. PRO ($9.99/mo):
   - All Free features.
   - Morris Kernel V3.5 Mastering UNLOCKED.
   - Quota: 20 High-Res WAV downloads / month (tracked in DB).
   - Unlimited Mastered MP3 downloads.
   - Audio Converter UNLOCKED.
   - IP Provenance Certificates LOCKED.
3. KING ($24.99/mo):
   - All Pro features.
   - Unlimited 24-bit WAV and MP3 downloads.
   - JAX HMAC-SHA256 IP Provenance Certificates UNLOCKED (court-ready PDF generation).
   - Forensic Signal Verification UNLOCKED.
4. ADMIN BYPASS:
   - If request or user session matches Master Admin Key, bypass all quotas, paywalls, and locks unconditionally.

Halt and output "STEP-BY-STEP REFACTOR READY" for verification before writing breaking changes.
